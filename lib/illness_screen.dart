import 'package:flutter/material.dart';

class IllnessScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Illness Screen'),
      ),
      body: Center(
        child: Text('Welcome to the Illness Screen!'),
      ),
    );
  }
}